# Server
PORT=3000
# Production: https://tokenlens-mcp-production.up.railway.app
ADMIN_SECRET=

# AI provider keys (optional — only needed for server-side AI features)
# The Chrome Extension sends the user's own key per-request; no server key required
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
